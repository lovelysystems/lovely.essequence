es:
    api:
        enabled: true
discovery:
    type: zen
    zen:
        ping:
            multicast:
                enabled:  false

# at least groovy update must be switched on
script:
    engine:
        groovy:
            inline:
                update: on

cluster:
    name: lovely.crate-essequence.testing
